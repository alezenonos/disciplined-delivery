# Report: Plugin metadata + cut release 0.2.0

| | |
|---|---|
| Date created | 2026-06-25 |
| Last edited | 2026-06-25 |
| Author | Claude Opus 4.8 (Claude Code) |
| Human partner | alezenonos |
| Status | In progress |
| Related | "pick it up as you see fit" — the plugin.json metadata + version follow-up |

## Task

Add the missing professional manifest metadata and cut the first real tagged release.

## Decisions

| Decision | Chosen | Why |
|----------|--------|-----|
| Version | **0.2.0** (not 0.1.0) | CHANGELOG already documents `0.1.0` as the baseline with a substantial `Unreleased` section (eval harness, install check, CI matrix); those are backward-compatible features → MINOR bump |
| `repository`/`homepage` format | string URLs | Accepted by `claude plugin validate` |
| `license` | `MIT` | Matches the repo `LICENSE` |
| Tagging | after merge, on `main` | A release tag should point at the merged commit, not a feature-branch commit |

## What was done

- `plugin.json`: added `homepage`, `repository`, `license`; bumped `version` 0.1.0 → 0.2.0.
- `CHANGELOG.md`: moved `Unreleased` → `## [0.2.0] - 2026-06-25` (added the metadata and
  dependabot-grouping lines); refreshed the compare/release link references.

## Verification / evidence

`claude plugin validate .` ✓ (new metadata fields accepted); `validate_manifests.py` +
`check_evals.py` ✓; `ruff` clean; `pytest` → 10 passed.

## Outstanding / next steps

- **After this merges**, cut the tag on `main`: `claude plugin tag --push` (creates
  `disciplined-delivery--v0.2.0`), or `git tag disciplined-delivery--v0.2.0 && git push --tags`.
- Remaining pro-plugin items (separate changes): `allowed-tools` on skills (a skill change →
  needs an eval run), eval transcripts (manual, clean session), community-marketplace submission.

## Change log

- 2026-06-25: created.
